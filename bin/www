const clusterise = require('../src/helpers/cluster.helper');
const status = clusterise();

if (status.clusterise == true) {
    if (status.isMaster == false) {
        require('../src/index');
    }
} else if (status.clusterise == false) {
    console.log('Starting app in non-cluster mode');
    require('../src/index');
}
